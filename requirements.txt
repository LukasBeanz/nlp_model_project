# Python 3.11 기준 패키지 목록
# PyTorch는 설치 환경(CPU/GPU, CUDA 버전)에 따라 설치 명령이 달라질 수 있습니다.
# 우선 PyCharm 터미널에서 아래 파일로 설치를 시도합니다.
# pip install -r requirements.txt

numpy>=1.26
pandas>=2.1
matplotlib>=3.8
scikit-learn>=1.3

torch>=2.2
torchvision>=0.17
torchaudio>=2.2

pytorch-lightning>=2.2
torchmetrics>=1.3

torchtext>=0.17

# IMDB 등 표준 데이터셋 로드용 (lstm_imdb_lightning.py 현대화 버전에서 사용)
datasets>=2.18
